{
  "version": 1,
  "mode": "RESEARCH_INTAKE_ONLY",
  "calls": [],
  "schema": {
    "required": ["caller", "source", "contract", "network", "called_at"],
    "notes": "Untrusted alpha calls enter here first. A caller signal never bypasses Wallet500 identity, market-data, liquidity, risk, and confluence gates. Missing contract/network/timestamp means reject or hold as UNVERIFIED. No auto-trade."
  }
}
