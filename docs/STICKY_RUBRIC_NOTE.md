# Sticky experiment rubric

The experiment rubric must remain accessible while scrolling on every public Wallet500 dashboard. Mobile uses horizontal scrolling inside the rubric. This is navigation-only and does not change research, scoring, or execution logic.
