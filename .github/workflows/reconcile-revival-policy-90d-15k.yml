name: Wallet500 Reconcile Revival Policy 90D 15K [DEPRECATED]

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: wallet500-reconcile-revival-policy-90d-15k-deprecated
  cancel-in-progress: true

jobs:
  deprecated:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Fail closed — legacy production rollback is prohibited
        run: |
          echo '::error::DEPRECATED_POLICY_WRITER_BLOCKED: production truth is 180d / $50K with exact identity and exact pair.'
          exit 1
