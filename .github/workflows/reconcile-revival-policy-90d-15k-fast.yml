name: Wallet500 Reconcile Revival Policy 90D 15K Fast — DEPRECATED

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: wallet500-reconcile-revival-policy-90d-15k-deprecated
  cancel-in-progress: true

jobs:
  deprecated:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Fail closed — obsolete production policy migration
        shell: bash
        run: |
          set -euo pipefail
          echo '::error::DEPRECATED_POLICY_MIGRATION_BLOCKED'
          echo 'This workflow is intentionally disabled because it rewrites canonical production truth from 180d/$50K to legacy 90d/$15K semantics.'
          echo 'Research lanes may keep explicitly isolated research thresholds, but this workflow must never mutate production policy, tests, dashboard, or publishers.'
          exit 1
