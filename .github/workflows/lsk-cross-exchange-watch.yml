name: Wallet500 LSK Cross-Exchange Watch (PAUSED)

# Paused by user request on 2026-09-16.
# Keep the workflow/code for future research reuse, but do not schedule it,
# do not run on push, and do not expose Telegram secrets.
on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: wallet500-lsk-cross-exchange-watch
  cancel-in-progress: true

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: LSK cross-exchange Telegram watch is paused
        run: echo 'LSK_CROSS_EXCHANGE_TELEGRAM_WATCH_PAUSED'
