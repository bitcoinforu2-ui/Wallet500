[project]
name = "wallet500"
version = "0.1.0"
description = "Multi-Chain Early Market Intelligence Engine"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
